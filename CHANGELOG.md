# 更新日志

本文件记录第 25 组 PR03-01“条件启动子生成”项目的阶段性进展。凡是影响数据版本、数据划分、模型结构、评价标准、实验结果或可复现流程的改动，都必须在合并到 `main` 时同步更新。

日期采用 `YYYY-MM-DD` 格式；每条记录应说明做了什么、为什么做、产生了什么可复核结果，并链接到对应代码、配置、报告或提交。

## 当前阶段：M3 条件生成最小闭环

### 2026-09-18 — 条件位置频率生成基线完成

- 新增按训练集弱、中、强表达组分别学习位置碱基频率的条件生成器；
- 每个条件生成 500 条序列，均为合法、互不重复且未直接复制本组训练序列；弱、中、强生成序列的 GC 均值依次为 `0.4571`、`0.4454`、`0.4302`，与各自训练组的方向一致；
- 该模型证明条件输入能够改变生成分布，但不能证明候选的真实表达强度，也不学习完整 motif；
- 结果见 [M3 条件位置频率生成基线报告](reports/m3_conditional_position_baseline_2026-09-18.md)。

### 2026-09-17 — M3 无条件生成最小闭环建立

- 新增按位置独立碱基频率采样的无条件生成基线，以及合法性、唯一性、新颖性、GC、位置碱基频率和 3-mer 分布评价；
- 使用训练集生成 1,000 条候选，全部为合法且训练集外的 50 bp 序列，生成 GC 均值为 `0.4420`，接近训练集 `0.4422`；
- 该基线不学习相邻位置依赖或表达强度条件，只作为后续条件生成器的最低比较标准；
- 结果见 [M3 位置频率生成基线报告](reports/m3_position_frequency_baseline_2026-09-17.md)。

### 2026-09-16 — 多尺度 CNN 首轮验证

- 实现同时使用 5 bp、7 bp、9 bp motif 窗口的多尺度位置感知 CNN；
- 验证集 MAE 为 `0.4379`、Pearson 为 `0.2147`，未超过单尺度 7 bp CNN，因此保留为可复现负结果，不作为当前主模型；
- 关联代码：`scripts/train_multiscale_motif_cnn.py`。

### 2026-09-13 — 位置感知强度预测器首轮探索

- 新增位置岭回归、位置 MLP 和带位置分区的 motif CNN 三种可复现候选；
- 位置岭回归与位置 MLP 均未超过全局 k-mer 岭回归；CNN 在验证集达到 MAE `0.4317`、Pearson `0.2607`，但开发期测试集 Pearson 为 `0.2139`，尚不满足冻结为独立评估器的条件；
- 已将开发期测试集结果与限制写入 [位置预测器探索报告](reports/m2_position_predictor_2026-09-17.md)，后续调参只依据验证集；
- 关联代码：`scripts/train_position_strength_predictor.py`、`scripts/train_position_mlp_predictor.py`、`scripts/train_motif_cnn_predictor.py`。
- Git 提交：`6db4702`。

### 2026-09-14 — motif CNN 验证集选择完成

- 以验证集 MAE 为唯一主选择指标，比较 5 bp、7 bp、9 bp 卷积窗口与 32/64 过滤器配置；
- 选定 7 bp 卷积窗口、32 个过滤器、五个位置区间和 64 单元预测头，固定配置见 `configs/m2_motif_cnn_selected.toml`；
- 固定结构后测试集 Pearson 为 `0.2139`，未超过全局 k-mer 基线，因此 CNN 仍是后续改进的候选结构，不作为最终独立评估器；
- 结果见 [CNN 选择记录](reports/m2_motif_cnn_selection_2026-09-17.md)。

### 2026-09-11 — 正式 EDA 完成

- 新增可复现 EDA 入口 [run_eda.py](scripts/run_eda.py)；
- 输出主数据的对数强度分布、位置碱基频率、不同强度组 GC 含量，以及数据字典和建模约束；
- 确认训练、验证、测试集合的 `log10(强度)` 与 GC 分布整体接近；
- 确认位置碱基频率不均匀，因此后续强度预测器需要使用位置特征；
- 结果见 [正式 EDA 报告](reports/eda/ecoli_formal_eda.md)；
- Git 提交：`c846015`。

### 2026-09-09 — 固定相似性划分与强度预测基线完成

- 比较 80%、85%、90% 的对齐汉明相似度阈值；
- 生成 90% 阈值的固定相似性簇划分：训练集 8,318 条、验证集 1,783 条、测试集 1,783 条；
- 所有相似性簇均只出现在一个集合中；
- 训练集专用的 `log10(强度)` 均值、标准差和三等分边界已保存；
- 固定划分上的 1–3-mer 岭回归基线测试集 Pearson 为 0.2349，说明全局 k-mer 统计不足以作为最终评估器；
- 结果见 [M2 划分与基线报告](reports/m2_split_and_strength_baseline_2026-09-17.md)；
- Git 提交：`7492d1c`。

## 已完成阶段：M1 问题分析

### 2026-09-07 — 课程数据复核与工程基础

- 建立统一配置、日志、随机种子、课程数据读取、特征提取和岭回归开发基线；
- 核验 11,884 条大肠杆菌连续强度主数据全部为 50 bp；
- 区分主数据与 80/81 bp 的大肠杆菌启动子二分类数据，避免混合训练；
- 结果见 [课程数据复核](reports/data_audit_2026-09-17.md) 与 [大肠杆菌数据说明](reports/ecoli_data_overview_2026-09-17.md)；
- Git 提交：`dcf2792`、`4ed330e`、`32956d7`。

## 后续记录模板

```markdown
### YYYY-MM-DD — 简短标题

- 完成内容：
- 关键决定及原因：
- 结果与限制：
- 关联文件：
- Git 提交：
```

## 下一步

- [x] 实现位置感知强度预测器，并在固定测试集上报告开发期结果；
- [ ] 仅以验证集完善 motif CNN，并冻结可用的独立评估器及统一评价接口；
- [x] 实现无条件生成基线，进入 M3 最小闭环。
- [x] 实现按弱、中、强等级工作的条件生成基线。
- [ ] 实现条件 VAE 与条件自回归生成器，并在统一评价框架下比较。
