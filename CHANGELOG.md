# 更新日志

本文件记录第 25 组 PR03-01“条件启动子生成”项目的阶段性进展。凡是影响数据版本、数据划分、模型结构、评价标准、实验结果或可复现流程的改动，都必须在合并到 `main` 时同步更新。

日期采用 `YYYY-MM-DD` 格式；每条记录应说明做了什么、为什么做、产生了什么可复核结果，并链接到对应代码、配置、报告或提交。

## 已完成阶段：M1 问题分析

### 2026-09-07 — 课程数据复核与工程基础

- 建立统一配置、日志、随机种子、课程数据读取、特征提取和岭回归开发基线；
- 核验 11,884 条大肠杆菌连续强度主数据全部为 50 bp；
- 区分主数据与 80/81 bp 的大肠杆菌启动子二分类数据，避免混合训练；
- 结果见 [课程数据复核](reports/data_audit_2026-09-17.md) 与 [大肠杆菌数据说明](reports/ecoli_data_overview_2026-09-17.md)；
- Git 提交：`dcf2792`、`4ed330e`、`32956d7`。

## 已完成阶段：M2 数据与评估基础

### 2026-09-09 — 固定相似性划分与强度预测基线完成

- 比较 80%、85%、90% 的对齐汉明相似度阈值；
- 生成 90% 阈值的固定相似性簇划分：训练集 8,318 条、验证集 1,783 条、测试集 1,783 条；
- 所有相似性簇均只出现在一个集合中；
- 训练集专用的 `log10(强度)` 均值、标准差和三等分边界已保存；
- 固定划分上的 1–3-mer 岭回归基线测试集 Pearson 为 0.2349，说明全局 k-mer 统计不足以作为最终评估器；
- 结果见 [M2 划分与基线报告](reports/m2_split_and_strength_baseline_2026-09-17.md)；
- Git 提交：`7492d1c`。

### 2026-09-11 — 正式 EDA 完成

- 新增可复现 EDA 入口 [run_eda.py](scripts/run_eda.py)；
- 输出主数据的对数强度分布、位置碱基频率、不同强度组 GC 含量，以及数据字典和建模约束；
- 确认训练、验证、测试集合的 `log10(强度)` 与 GC 分布整体接近；
- 确认位置碱基频率不均匀，因此后续强度预测器需要使用位置特征；
- 结果见 [正式 EDA 报告](reports/eda/ecoli_formal_eda.md)；
- Git 提交：`c846015`。

### 2026-09-13 — 位置感知强度预测器首轮探索

- 新增位置岭回归、位置 MLP 和带位置分区的 motif CNN 三种可复现候选；
- 位置岭回归与位置 MLP 均未超过全局 k-mer 岭回归；CNN 在验证集达到 MAE `0.4317`、Pearson `0.2607`，但开发期测试集 Pearson 为 `0.2139`，尚不满足冻结为独立评估器的条件；
- 已将开发期测试集结果与限制写入 [位置预测器探索报告](reports/m2_position_predictor_2026-09-17.md)，后续调参只依据验证集；
- 关联代码：`scripts/train_position_strength_predictor.py`、`scripts/train_position_mlp_predictor.py`、`scripts/train_motif_cnn_predictor.py`。
- Git 提交：`6db4702`。

### 2026-09-14 — motif CNN 验证集选择完成

- 以验证集 MAE 为唯一主选择指标，比较 5 bp、7 bp、9 bp 卷积窗口与 32/64 过滤器配置；
- 选定 7 bp 卷积窗口、32 个过滤器、五个位置区间和 64 单元预测头，固定配置见 `configs/m2_motif_cnn_selected.toml`；
- 固定结构后测试集 Pearson 为 `0.2139`，未超过全局 k-mer 基线，因此 CNN 仍是后续改进的候选结构，不作为最终独立评估器；
- 结果见 [CNN 选择记录](reports/m2_motif_cnn_selection_2026-09-17.md)。

### 2026-09-16 — 多尺度 CNN 首轮验证

- 实现同时使用 5 bp、7 bp、9 bp motif 窗口的多尺度位置感知 CNN；
- 验证集 MAE 为 `0.4379`、Pearson 为 `0.2147`，未超过单尺度 7 bp CNN，因此保留为可复现负结果，不作为当前主模型；
- 关联代码：`scripts/train_multiscale_motif_cnn.py`。

## 当前阶段：M3 条件生成最小闭环

### 2026-09-17 — M3 无条件生成最小闭环建立

- 新增按位置独立碱基频率采样的无条件生成基线，以及合法性、唯一性、新颖性、GC、位置碱基频率和 3-mer 分布评价；
- 使用训练集生成 1,000 条候选，全部为合法且训练集外的 50 bp 序列，生成 GC 均值为 `0.4420`，接近训练集 `0.4422`；
- 该基线不学习相邻位置依赖或表达强度条件，只作为后续条件生成器的最低比较标准；
- 结果见 [M3 位置频率生成基线报告](reports/m3_position_frequency_baseline_2026-09-17.md)。

### 2026-09-18 — 条件位置频率生成基线完成

- 新增按训练集弱、中、强表达组分别学习位置碱基频率的条件生成器；
- 每个条件生成 500 条序列，均为合法、互不重复且未直接复制本组训练序列；弱、中、强生成序列的 GC 均值依次为 `0.4571`、`0.4454`、`0.4302`，与各自训练组的方向一致；
- 该模型证明条件输入能够改变生成分布，但不能证明候选的真实表达强度，也不学习完整 motif；
- 结果见 [M3 条件位置频率生成基线报告](reports/m3_conditional_position_baseline_2026-09-18.md)。

### 2026-09-18 — 条件自回归生成基线完成

- 新增三阶条件自回归生成器，按强度等级、位置和前三个已生成碱基逐位采样；
- 三组候选均为合法、唯一且未直接复制训练序列；3-mer JS 散度约为 `0.0003`，显著低于条件位置频率基线的 `0.005`—`0.007`；
- 该模型是当前更强的生成基线，但仍只处理离散三等级条件和三碱基局部上下文；
- 结果见 [M3 条件自回归基线报告](reports/m3_conditional_autoregressive_baseline_2026-09-18.md)。

### 2026-09-19 — 连续条件 VAE 首轮实现

- 新增连续 `log10(强度)` 条件的 VAE，包括编码、重参数采样、解码、验证集早停和按目标强度采样；
- 将 KL 权重从 `0.01` 调整至 `0.10` 后，生成位置碱基 L1 差异从约 `0.46` 降至约 `0.19`；
- 当前 VAE 仍弱于条件自回归基线的局部 3-mer 保真度，因此作为可复现候选继续改进，不冻结为主生成器；
- 结果见 [M3 连续条件 VAE 首轮报告](reports/m3_conditional_vae_first_run_2026-09-19.md)。

### 2026-09-19 — M3 两种条件生成器统一比较完成

- 新增 5-mer 候选 motif 保真度、生成多样性、最近训练序列相似度等统一生成评价；
- 条件自回归的平均 3-mer JS 散度为 `0.00033`，优于 VAE 的 `0.00630`；高频 5-mer 重合率为 `0.62`，优于 VAE 的 `0.43`；
- 当前将条件自回归定位为 M3 主生成基线，连续条件 VAE 定位为待优化的连续控制候选；
- 结果见 [M3 条件生成器统一比较报告](reports/m3_conditional_generator_comparison_2026-09-19.md)。

### 2026-09-20 — 连续条件 VAE 响应分析完成

- 在 1.7 至 2.9 的 7 个连续目标强度上生成候选，并检查生成分布变化；
- 三个随机种子下，目标强度与生成 GC 均值的 Pearson 均为负，范围 `-0.953` 至 `-0.833`，平均为 `-0.891`；
- 相邻目标仍有局部波动，且不同种子的绝对 GC 水平存在差异，因此当前结论是“存在连续条件响应趋势”，不是“已实现精确连续强度控制”；
- 结果见 [M3 连续条件 VAE 响应报告](reports/m3_vae_continuous_response_2026-09-20.md)。

### 2026-09-20 — VAE KL 权重稳定性消融完成

- 比较 beta 为 `0.05`、`0.10`、`0.20` 的 VAE，每种配置使用三个随机种子；
- beta = `0.20` 的平均 3-mer JS 散度最低（`0.00455`），连续目标—GC 响应最强且最稳定，因此作为下一轮默认候选；
- beta = `0.20` 的验证重构损失较高，说明生成模型的选择不能只按重构损失；
- 结果见 [M3 VAE KL 权重稳定性报告](reports/m3_vae_beta_stability_2026-09-20.md)。

### 2026-09-21 — VAE 潜变量维度消融完成

- 在 beta = `0.20` 下比较潜变量维度 `8`、`16`、`32`，每种配置使用三个随机种子；
- 16 维的平均 3-mer JS 散度最低（`0.00455`），连续响应最强，因此与 beta = `0.20` 一起固定为后续默认候选；
- 32 维验证重构损失最低但生成质量最差，再次说明重构损失不能替代生成质量评价；
- 结果见 [M3 VAE 潜变量维度稳定性报告](reports/m3_vae_latent_stability_2026-09-21.md)。

### 2026-09-21 — VAE 网络宽度消融与 M3 阶段复查完成

- 固定 `beta = 0.20`、`latent_size = 16`，以三个随机种子比较隐藏层宽度 `96` 和 `128`；
- `hidden_size = 128` 的平均 3-mer JS 散度为 `0.00442`，目标—GC Pearson 为 `-0.870 ± 0.034`，优于 96 宽度，因此成为当前默认配置；
- 重新以选定配置比较两种生成器：条件自回归平均 3-mer JS 为 `0.00033`，VAE 为 `0.00376`，M3 主生成基线仍为条件自回归；
- 复查确认本轮仅使用训练集和验证集，测试集未参与 M3 选型；同步修正 VAE 训练与比较入口的默认参数；
- 结果见 [M3 VAE 网络宽度稳定性报告](reports/m3_vae_width_stability_2026-09-21.md) 与 [M3 阶段复查记录](reports/m3_review_2026-09-21.md)。

### 2026-09-22 — M3 汇报证据包固化

- 新增发布检查入口，核对固定数据划分、选定 VAE 配置、生成有效性和 M3 主模型结论；
- 整理不依赖 PPT 的期中汇报工作简报，统一项目简介、方法表述、关键结果、限制和常见问答；
- 本轮不引入新模型、不使用测试集调参，也不进入 M4；
- 结果见 [M3 期中汇报工作简报](reports/m3_midterm_brief_2026-09-22.md)。
## 后续记录模板

```markdown
### YYYY-MM-DD — 简短标题

- 完成内容：
- 关键决定及原因：
- 结果与限制：
- 关联文件：
- Git 提交：
```

## 下一步

- [x] 实现位置感知强度预测器，并在固定测试集上报告开发期结果；
- [ ] 仅以验证集完善 motif CNN，并冻结可用的独立评估器及统一评价接口；
- [x] 实现无条件生成基线，进入 M3 最小闭环。
- [x] 实现按弱、中、强等级工作的条件生成基线。
- [x] 实现条件自回归生成基线，并完成与位置独立基线的分布比较。
- [x] 实现连续条件 VAE 与条件自回归生成器，并在统一评价框架下完成首轮比较。
- [x] 补充 motif 与多样性评价，并冻结 M3 首轮对比结论。
- [ ] 使用验证集完善连续条件 VAE，补充更细粒度的连续条件响应分析。
- [x] 完成连续条件 VAE 的首轮多目标响应分析。
- [ ] 在多个随机种子下复核 VAE 条件响应，并改善局部 motif 保真度。
- [x] 完成 VAE KL 权重的多随机种子稳定性消融。
- [x] 在 beta = 0.20 下比较潜变量维度和网络宽度，并复核连续条件响应。
- [x] 在 beta = 0.20 下完成潜变量维度的多随机种子消融。
- [x] 比较网络宽度 96 与 128，并完成 M3 阶段复查。
